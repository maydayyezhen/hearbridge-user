/**
 * 路由纯路径模式表。
 *
 * 说明：
 * 1. 这里只维护匹配层依赖的纯路径元数据。
 * 2. 不从 RouteRegistry 派生，避免把渲染注册表反向拉入 matcher。
 */
import {
  COURSE_DETAIL_PATH,
  HOME_PATH,
  LAUNCH_PATH,
  LOGIN_PASSWORD_PATH,
  LOGIN_SMS_PATH,
  PROFILE_ABOUT_PATH,
  PROFILE_ACCOUNT_PATH,
  PROFILE_FAVORITES_PATH,
  PROFILE_FEEDBACK_PATH,
  PROFILE_PASSWORD_PATH,
  PROFILE_PATH,
  PROFILE_SECURITY_PATH,
  RECORD_PATH,
  REGISTER_PATH,
  TRAIN_PATH,
  TRAIN_SESSION_COURSE_PATH,
  TRAIN_SESSION_PATH
} from './RoutePaths'
import { AppRoutePath } from './RouteTypes'

export const ROUTE_PATTERNS: AppRoutePath[] = [
  LAUNCH_PATH,
  LOGIN_SMS_PATH,
  LOGIN_PASSWORD_PATH,
  REGISTER_PATH,
  HOME_PATH,
  TRAIN_PATH,
  RECORD_PATH,
  PROFILE_PATH,
  PROFILE_FAVORITES_PATH,
  PROFILE_ACCOUNT_PATH,
  PROFILE_SECURITY_PATH,
  PROFILE_PASSWORD_PATH,
  PROFILE_FEEDBACK_PATH,
  PROFILE_ABOUT_PATH,
  COURSE_DETAIL_PATH,
  TRAIN_SESSION_COURSE_PATH,
  TRAIN_SESSION_PATH
]

export const ROOT_TAB_PATHS: AppRoutePath[] = [
  HOME_PATH,
  TRAIN_PATH,
  RECORD_PATH,
  PROFILE_PATH
]
