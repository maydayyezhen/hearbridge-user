# 项目整改与自检结果 v35

基于：`ets_record_route_cleanup_v34_doc_order_refactored(2).zip`
对应文档：`项目结构评估与整改文档_v34(1).md`

说明：
- 本次按文档中的推荐整改顺序，连续完成 5 步整改。
- 本次仍以**结构整改 / 分层边界 / 状态流 / 页面职责**为主，不包含语法报错逐条修复。
- 自检以静态结构检查、全局检索与守卫脚本结果为准。

---

## 第 1 步：先收训练流程编排

### 本步整改内容

新增：
- `services/training/local/TrainStageFlowService.ets`
- `pages/train/flow/TrainPageController.ets`

改造：
- `services/training/index.ets`
- `services/training/local/TrainPageLocalService.ets`
- `pages/train/flow/TrainPage.ets`

本步完成的核心动作：
1. 抽出训练阶段推进规则，集中到 `TrainStageFlowService.ets`。
2. 将 training 模块正式出口收敛为：
   - `TrainPageActions`
   - `TrainPageQueries`
3. 把训练页中的 UI glue（语音服务、picker、防抖、阶段动作包装）下沉到 `TrainPageController.ets`。

### 自检结果

检查项：
- `TrainStageFlowService.ets` 已存在，训练页前进 / 回退规则已集中。
- `TrainPageController.ets` 已存在。
- `TrainPage.ets` 已改为使用 `TrainPageActions / TrainPageQueries`，不再散落 import 多个零散能力。

结论：
- **通过**。
- 训练页主流程的改动落点已经明显收敛，后续改阶段规则时不再需要来回翻多处页面 glue 代码。

---

## 第 2 步：再收训练状态所有权

### 本步整改内容

新增：
- `services/training/query/TrainPageScaffoldQueryService.ets`

改造：
- `storage/train-session/TrainSessionViewStateStore.ets`
- `repository/train-session/TrainSessionViewStateRepository.ets`
- `services/training/local/TrainSessionViewStateService.ets`
- `services/training/local/TrainSessionBootstrapLocalService.ets`
- `pages/train/flow/TrainPage.ets`
- `pages/train/flow/stages/TrainIntroStagePage.ets`

本步完成的核心动作：
1. 将训练页课程展示信息改为 query 组装，不再放在 `TrainSessionViewStateStore`。
2. `TrainSessionViewStateStore` 只保留临时 UI 状态：
   - `speechPartialText`
   - `speechStatusText`
   - `trainingTip`
3. `TrainPage` / `TrainIntroStagePage` 课程展示改为从 `TrainPageQueries.getCourseDisplay(...)` 获取。

### 自检结果

检查项：
- `TrainSessionViewStateStore.ets` 仅剩 3 个字段：`speechPartialText / speechStatusText / trainingTip`。
- 在：
  - `storage/train-session/TrainSessionViewStateStore.ets`
  - `repository/train-session/TrainSessionViewStateRepository.ets`
  - `services/training/local/TrainSessionViewStateService.ets`
  中检索 `courseName / courseIntro / courseCoverUrl`，结果为 **CLEAN**。
- `pages/train/flow/TrainPage.ets` 与 `pages/train/flow/stages/TrainIntroStagePage.ets` 已通过 query 读取课程展示信息。

结论：
- **通过**。
- 训练页临时视图态与课程展示信息已完成拆分，状态所有权更清晰。

---

## 第 3 步：再收路由耦合

### 本步整改内容

新增：
- `router/RoutePatternRegistry.ets`
- `router/RouteRenderer.ets`
- `pages/train/root/TrainRootRouteAdapter.ets`
- `pages/train/detail/CoursePageRouteAdapter.ets`

改造：
- `router/RouteMatcher.ets`
- `pages/layout/AppRouteViewport.ets`
- `pages/train/root/TrainRootPage.ets`
- `pages/train/detail/CoursePage.ets`
- `services/course/CourseDetailPageService.ets`

本步完成的核心动作：
1. 将纯路由模式表拆到 `RoutePatternRegistry.ets`。
2. `RouteMatcher.ets` 不再依赖 `RouteRegistry`。
3. 将页面 routePath 解析集中到各自的 `*RouteAdapter.ets`。
4. `CourseDetailPageService.ets` 不再处理 `routePath` / `getRouteParam`。

### 自检结果

检查项：
- `RouteMatcher.ets` 中已不再引用 `RouteRegistry`，结果为 **CLEAN**。
- `TrainRootRouteAdapter.ets / CoursePageRouteAdapter.ets / TrainPageRouteAdapter.ets` 已存在。
- `CourseDetailPageService.ets` 中检索 `routePath|getRouteParam`，结果为 **CLEAN**。

结论：
- **通过**。
- 路由匹配、路由渲染、页面参数解析的职责已拆开，路由基础设施层更纯。

---

## 第 4 步：补纯度问题

### 本步整改内容

新增：
- `services/user/local/UserFavoriteStateService.ets`
- `services/training-item/local/TrainingItemDetailLocalService.ets`

改造：
- `repository/user/UserRepository.ets`
- `services/user/api/UserFavoriteApiService.ets`
- `repository/training-item/TrainingItemDetailRepository.ets`
- `repository/training-item/TrainingItemPreviewRepository.ets`
- `services/ui/local/ToastService.ets`
- `services/training/local/TrainStageLocalService.ets`

本步完成的核心动作：
1. 将用户收藏集合替换 / 去重规则上移到 `UserFavoriteStateService.ets`。
2. 将训练项详情 merge 规则上移到 `TrainingItemDetailLocalService.ets`。
3. `UserRepository.ets` 回到基础读写与只读事实查询。
4. `TrainingItemDetailRepository.ets` 回到基础读写与实体转换。
5. 清理 service 层对 storage 的直接 import。

### 自检结果

检查项：
- 全量检索 `services` 目录中的 `import ... storage/...`，结果为 **CLEAN**。
- `UserRepository.ets` 中检索：
  - `applyCourseFavoriteInUser`
  - `applyFavoriteItemInUser`
  - `applyCourseFavoriteState`
  - `applyTrainingItemFavoriteState`
  结果为 **CLEAN**。
- `TrainingItemDetailRepository.ets` 中检索 `mergeDetailEntry / mergeTrainingItemDetailEntry`，结果为 **CLEAN**。

结论：
- **通过**。
- repository / service 的边界更纯，后续继续扩业务时不容易再把 repository 用成半个 service。

---

## 第 5 步：最后做页面组件拆分

### 本步整改内容

新增组件文件：
- `pages/home/components/HomeHeroCard.ets`
- `pages/home/components/HomeRecommendCard.ets`
- `pages/home/components/HomeStatusCard.ets`
- `pages/profile/components/ProfileOverviewCard.ets`
- `pages/profile/components/ProfileMenuList.ets`
- `pages/train/root/components/CourseCoverImage.ets`
- `pages/train/root/components/CourseCardList.ets`
- `pages/train/root/components/CourseListFilterBar.ets`
- `pages/train/flow/components/TrainStageTopArea.ets`
- `pages/train/flow/components/TrainTopActionsBar.ets`
- `pages/train/flow/components/TrainBottomBar.ets`
- `pages/train/flow/components/TrainOutlinePanel.ets`

改造：
- `pages/home/HomePage.ets`
- `pages/profile/ProfilePage.ets`
- `pages/train/root/CourseListPage.ets`
- `pages/train/flow/components/TrainFlowPanels.ets`

### 自检结果

检查项：
- `CourseListPage.ets` 中 `@Component` 数量：**1**
- `HomePage.ets` 中 `@Component` 数量：**1**
- `ProfilePage.ets` 中 `@Component` 数量：**1**
- 新增拆分组件文件总数：**14**

结论：
- **通过**。
- 页面层已回到“页面组装 + 路由承接 + 状态接线”为主，组件边界更清晰。

---

## 最终总自检

### 守卫脚本
执行：
- `bash structure_guard_check.sh`

结果：
- **结构守卫检查通过**

### 最终结论

本次 5 步整改已按文档顺序完成。

当前结果可以概括为：
1. **训练流程编排收口完成**：阶段规则、训练页动作出口、页面 glue 已分层。
2. **训练状态所有权更清晰**：训练页 view state 不再混课程展示字段。
3. **路由耦合降低**：匹配、渲染、页面参数解析职责拆开。
4. **repository / service 边界更纯**：解释型规则已上移，service 不再直连 storage。
5. **页面组件层更清楚**：Home / Profile / CourseList / TrainFlow 的页面与组件已经拆开。

当前这版比整改前更适合继续联调后端，也更适合你后面自己回来看和继续收尾。
