# 整改执行与自检结果 v39

> 基于 `ets_record_route_cleanup_v37_reworked_all_steps_p1_p3_done` 与 `下一位AI整改任务书_v39` 执行。

## 本轮完成顺序
1. P0 修正文档与现状漂移
2. P1 清理 requestApi 上层历史性 catch
3. P1 收紧训练链路关键弱类型
4. P2 补新的守卫与行为级自检

## 本轮代码触达目录
- `services/`
- `pages/`
- `domain/`
- `storage/`
- 根目录脚本与 handoff 文档

## 本轮 README / 文档同步
已更新：
- `README.md`
- `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md`
- `services/README.md`
- `api/README.md`
- `pages/README.md`
- `storage/README.md`
- `docs/restructure/整改执行与自检结果_v39.md`

未改：
- `router/README.md`：本轮未改变路由职责边界，仅复用既有 adapter 规则
- `repository/README.md`：本轮未调整 repository 职责，仅继续沿用当前说明

## Step 1：P0 修正文档与现状漂移
### 触达文件
- `README.md`
- `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md`
- `services/README.md`
- `api/README.md`
- `pages/README.md`
- `storage/README.md`

### 本步动作
- 重写 handoff，移除“请求层仍抛普通网络异常”“TrainPageService 仍解析 routePath”等过期问题表述
- 在 README 中明确当前下一阶段重点是：上层旧风格清理、弱类型收口、训练模块复杂度治理
- 同步守卫脚本说明与页面 / service 结果承接约定

### 自检方法
- 手动核对 README 与当前代码职责是否一致
- 手动核对 handoff 是否只保留真实剩余项

### 自检结果
- 通过：文档已与当前代码现状对齐
- 通过：已完成项不再被写成当前待整改项

## Step 2：P1 清理 requestApi 上层历史性 catch
### 触达文件
- `services/auth/api/AuthApiService.ets`
- `services/course/api/CourseListApiService.ets`
- `services/record/api/RecordApiService.ets`
- `services/home/api/HomeApiService.ets`
- `services/user/api/UserAccountActionService.ets`
- `services/user/api/UserProfileSaveApiService.ets`
- `pages/auth/PasswordLoginPage.ets`
- `pages/auth/RegisterPage.ets`
- `pages/auth/SmsLoginPage.ets`
- `pages/train/root/CourseListPage.ets`

### 本步动作
- 纯请求 service 改为优先按 `result.ok` 承接，不再为普通网络失败保留旧式 catch
- 登录页 / 注册页 / 短信登录页 / 课程列表页改为直接消费 service 返回结果对象
- 保留 `UserAccountActionService` / `UserProfileSaveApiService` 中少量 catch，但仅用于：
  - 读取 / 清理登录态持久化
  - 头像/头图上传这类本地媒体能力与上传前置流程

### 自检方法
- `rg -n 'catch' services/auth/api/AuthApiService.ets services/course/api/CourseListApiService.ets services/record/api/RecordApiService.ets services/home/api/HomeApiService.ets`
- `rg -n 'catch' pages/auth/PasswordLoginPage.ets pages/auth/RegisterPage.ets pages/auth/SmsLoginPage.ets pages/train/root/CourseListPage.ets`
- 手动检查保留 catch 的文件是否属于持久化 / 媒体上传场景

### 自检结果
- 通过：纯请求 service 已移除普通网络失败 catch
- 通过：直接承接这些 service 的页面已移除旧式网络 catch
- 通过：保留 catch 的位置仅剩 `UserAccountActionService` 与 `UserProfileSaveApiService`，原因明确且属于本地持久化 / 媒体上传

## Step 3：P1 收紧训练链路关键弱类型
### 触达文件
- `services/training/TrainPageService.ets`
- `services/training/index.ets`
- `pages/train/flow/TrainPageController.ets`
- `pages/train/flow/TrainPageRouteAdapter.ets`
- `services/training/local/TrainPageLocalService.ets`
- `services/training/local/TrainSpeechLocalService.ets`
- `services/training/local/TrainSessionBootstrapContextLocalService.ets`
- `services/training/local/TrainSessionBootstrapLocalService.ets`
- `services/training/local/TrainSessionSubmitFlowLocalService.ets`
- `domain/entity/Course.ets`
- `domain/entity/UserTrainingItem.ets`
- `storage/course/CourseSessionStore.ets`

### 本步动作
- 训练链路公共 context 签名改为 `common.BaseContext | undefined`
- `TrainSessionRouteInput.stage` / `TrainSessionBootstrapContext.nextStage` 改为 `TrainStage`
- `TrainPageRouteAdapter` 改为先规范化 route stage，再构造 route input
- `Course.trainingType` 改为 `TrainingType`
- `UserTrainingItem.trainingType / status` 改为 `TrainingType / TrainItemStatus`
- `CourseSessionItemSnapshot.status` 改为 `TrainItemStatus`

### 自检方法
- `rg -n 'Object|stage: string|nextStage: string|\(\) => Object' services/training pages/train/flow domain/entity storage/course`
- 手动核对共享类型导出与 route adapter 调用链

### 自检结果
- 通过：训练链路目标文件已无 `Object` / 裸字符串 stage 残留
- 通过：路由输入与 bootstrap context 已使用显式 `TrainStage`
- 通过：课程 / 训练项 / 课程会话快照已接入共享类型

## Step 4：P2 补新的守卫与行为级自检
### 触达文件
- `structure_guard_check.sh`
- `structure_smoke_check.sh`

### 本步动作
- 新增训练链路 Object 扩散检查
- 新增 route input / bootstrap context 回退成字符串阶段检查
- 新增纯请求 service 历史 catch 回流检查
- 新增页面侧旧式网络 catch 回流检查
- 更新 smoke，覆盖训练链路显式类型与 route stage 规范化出口

### 自检命令
```bash
bash structure_guard_check.sh
bash structure_smoke_check.sh
rg -n "catch" services/auth/api services/course/api services/record/api services/home/api pages/auth pages/train/root services/user/api -g'*.ets'
rg -n "Object|stage: string|nextStage: string|\(\) => Object" services/training pages/train/flow domain/entity storage/course -g'*.ets'
```

### 自检结果
- `bash structure_guard_check.sh`：通过
- `bash structure_smoke_check.sh`：通过
- 额外 grep：符合预期；剩余 catch 仅位于 `services/user/api/UserAccountActionService.ets` 与 `services/user/api/UserProfileSaveApiService.ets`

## 本轮剩余项
1. `UserAccountActionService.ets` 与 `UserProfileSaveApiService.ets` 仍保留 catch，但已缩到持久化 / 媒体上传范围；后续若把这些本地能力进一步下沉，可继续瘦身
2. 训练模块动作链路当前仍偏长，但本轮优先做的是收口与定型，没有继续拆目录
3. 启动初始化注册说明仅在文档层补约定，未改 `AppBootstrapLocalService.ets` 实现

## 下一轮建议顺序
1. 继续评估 `user/api` 中保留 catch 是否还能进一步下沉
2. 控制训练模块新动作只挂到 `TrainPageService` 正式出口，不新增纯转发薄壳
3. 若新增模块初始化逻辑，务必同步补到 `getAppInitializers()` 与 README
