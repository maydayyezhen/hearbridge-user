# 整改执行与自检结果 v42

## 本轮目标
按 `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md` 的推荐顺序，先修正文档失真，再修 `FavoritesPage` 的 router 导入风险，随后补守卫、补充 training 与 app bootstrap 的说明，并清理低价值残留文件。

---

## 第 1 步：修正文档与真实代码状态失真

### 本步改动
- 更新 `README.md`
- 更新 `pages/README.md`
- 更新 `services/README.md`
- 更新 `services/training/README.md`
- 新增 `services/app/README.md`
- 更新 `router/README.md`
- 重写 `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md`

### 本步结果
- 根 README 的优先级已经改为：文档真相源 → `FavoritesPage` router 导入风险 → 守卫补齐 → training 冻结扩张 → app bootstrap 模块化表达 → 清理残留。
- handoff 已不再把 `FavoritesPage` 直读仓储与 `AccountPage` 系统能力泄漏当当前主战场。
- `pages/README.md` 已新增“页面不允许通过目录 barrel 导入 router”的约定。
- `router/README.md` 已明确：当前不提供 `router/index.ets` 这类目录正式出口。
- `services/training/README.md` 已补充 `TrainPageQueryLocalService.ets` 当前兼有默认值工厂与退出态清理的现状说明，并再次强调“冻结扩张、只做收口”。
- `services/app/README.md` 已新增并写明：当前启动初始化按 `core / course / training / ui` 四组表达，不做推倒式改造。

### 本步自检
- 关键词自检通过：`README.md` 与 handoff 都已出现新的优先级表述。
- 关键词自检通过：`pages/README.md`、`router/README.md`、`services/training/README.md`、`services/app/README.md` 已出现对应边界说明。

---

## 第 2 步：修 `FavoritesPage` 的 router 目录导入风险

### 本步改动代码
- 更新 `pages/profile/FavoritesPage.ets`

### 解决的问题
- 收藏页顶部 import 不再通过 `from '../../router'` 依赖一个并不存在的目录 barrel。
- 返回逻辑改为显式从正式文件导入并调用：
  - `go` 来自 `router/Router.ets`
  - `PROFILE_PATH` 来自 `router/RoutePaths.ets`

### 本步自检
- `pages/profile/FavoritesPage.ets` 中已不存在 `from '../../router'`
- `pages/profile/FavoritesPage.ets` 中已不存在 `Router.go(RoutePaths.PROFILE)`
- 全文搜索 `rg -n "from '../../router'|from '../router'|from '../../../router'" pages services repository storage` 无结果

---

## 第 3 步：补守卫，禁止目录 barrel 直接导入 router

### 本步改动代码
- 更新 `structure_guard_check.sh`
- 更新 `structure_smoke_check.sh`

### 解决的问题
- 守卫脚本现在可以直接拦截：
  - `from '../../router'`
  - `from '../router'`
  - `from '../../../router'`
- 后续若有人再次把业务源码写成目录导入 router，静态检查会直接失败。

### 本步自检
- `structure_guard_check.sh` 已新增第 `[19]` 项：业务源码不得通过目录 barrel 直接导入 router。
- `structure_smoke_check.sh` 已补对 `FavoritesPage` 及业务源码的断言。

---

## 第 4 步：冻结 training 模块扩张，并补清启动初始化说明

### 本步改动文档
- 更新 `services/training/README.md`
- 更新 `services/README.md`
- 新增 `services/app/README.md`
- 更新 `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md`

### 本步结果
- training 模块后续策略已明确：只做收口，不新增横向薄层，不把 route 解析和展示控制再带回 service。
- `TrainPageQueryLocalService.ets` 当前“query + 默认值工厂 + 退出态清理”并存的事实已被写明，后续接手者不会再误把它当成可无限加职责的文件。
- app bootstrap 当前只要求按模块分组表达，不再推动推倒式重构。

### 本步自检
- `services/training/README.md` 中已存在“冻结扩张、只做收口”。
- `services/README.md` 中已写明 `AppBootstrapLocalService` 当前保持 `core / course / training / ui` 四组初始化分法。

---

## 第 5 步：清理低价值残留

### 本步改动
- 删除根目录历史残留文件 `基于`

### 本步自检
- 根目录文件列表中已不存在 `基于`

---

## 本轮改动文件清单

### 新增文件
- `services/app/README.md`
- `docs/restructure/整改执行与自检结果_v42.md`
- `docs/restructure/final_selfcheck_v42_guard.txt`
- `docs/restructure/final_selfcheck_v42_smoke.txt`

### 更新文件
- `README.md`
- `pages/README.md`
- `services/README.md`
- `services/training/README.md`
- `router/README.md`
- `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md`
- `pages/profile/FavoritesPage.ets`
- `structure_guard_check.sh`
- `structure_smoke_check.sh`

### 删除文件
- `基于`

---

## 本轮最终自检结果

### `structure_guard_check.sh`
```text
[1] TrainPageService 不再直接解析 routePath
[2] 训练路由解析收口在 TrainPageRouteAdapter
[3] RouteMatcher 只依赖纯路径模式真源
[4] requestApi 不再向上抛出普通网络异常
[5] 训练模块正式出口保持统一
[6] TrainSessionStore 不得混入课程/结果展示字段
[7] api 不得反向依赖 repository/services/pages/storage
[8] TrainSessionStore 与 CourseSessionStore 不得引入 TrainingTypeUtil
[9] pages 不得直连 auth/course/record 零散 PageService
[10] 训练链路公共签名不再扩散 Object
[11] route input 与 bootstrap context 不得回退成裸字符串 stage
[12] 纯请求 service 不得回流旧式 catch
[13] 直接承接纯请求 service 的页面不再保留普通网络失败 catch
[14] 课程与训练项缓存类型不得回退成裸字符串
[15] EntryAbility 不得主动申请训练专属麦克风权限
[16] user api service 不得直接读写 token 或处理上传前置
[17] FavoritesPage 不得重新直读 UserRepository
[18] AccountPage 不得重新直连系统图片选择 API
[19] 业务源码不得通过目录 barrel 直接导入 router
结构守卫检查通过
```

### `structure_smoke_check.sh`
```text
行为级 smoke check 通过
```

---

## 本轮故意没动的内容
- 没有重新拆 `FavoritesPage` / `AccountPage` 的历史边界问题，因为这两项当前代码已基本收口。
- 没有继续拆 training 主链路，只补说明与守卫。
- 没有推翻 `AppBootstrapLocalService`，只保持按模块分组表达。
- 没有新增 `router/index.ets` 这类仅用于兼容错误导入的 barrel 文件。
