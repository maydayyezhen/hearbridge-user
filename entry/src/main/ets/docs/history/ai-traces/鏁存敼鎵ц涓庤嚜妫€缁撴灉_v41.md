# 整改执行与自检结果 v41

## 本轮目标
按 `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md` 的推荐顺序，先对齐文档，再修 `FavoritesPage` 与 `AccountPage` 两个明确边界回退点，最后补 training 模块说明、启动初始化表达与结构守卫。

---

## 第 1 步：文档与真实代码对齐

### 本步改动
- 更新 `README.md`
- 更新 `pages/README.md`
- 更新 `services/README.md`
- 更新 `router/README.md`
- 更新 `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md`

### 本步结果
- 根 README 已把主战场改为：文档对齐、`FavoritesPage`、`AccountPage`、training 冻结扩张、启动初始化表达、守卫补齐。
- `pages/README.md` 已明确：页面不再直读 `repository/user/UserRepository`，资料页不再直连系统资源选择 API。
- `services/README.md` 已明确：页面读模型由 `model / query` 提供，training 模块按“冻结扩张、只做收口”推进。
- `router/README.md` 已明确：router 当前结构冻结，不再继续新增薄层。

### 本步自检
- 通过关键词检查确认上述 README 与 handoff 都已出现新的优先级与约束表述。

---

## 第 2 步：修 `FavoritesPage` 的边界回退

### 本步改动代码
- 新增 `services/user/model/FavoriteDisplayItem.ets`
- 新增 `services/user/model/README.md`
- 新增 `services/user/query/UserFavoritesPageQueryService.ets`
- 新增 `services/user/query/README.md`
- 更新 `services/user/index.ets`
- 更新 `pages/profile/FavoritesPage.ets`

### 解决的问题
- 页面不再直接 import `repository/user/UserRepository`
- 页面不再调用 `readUserState()`
- 页面不再内联定义 `FavoriteDisplayItem`
- 页面不再自己拼课程副标题、训练类型文案和头部统计

### 调整后的边界
- 页面通过 `@StorageLink('user')` 读取当前用户状态
- 收藏页头部与列表读模型由 `UserFavoritesPageQueryService` 统一组装
- 页面只保留 tab 切换、空态、返回与卡片渲染

### 本步自检
- `pages/profile/FavoritesPage.ets` 中已不存在：
  - `repository/user/UserRepository`
  - `readUserState(`
  - 内联 `FavoriteDisplayItem`
  - `buildCourseItems()` / `buildTrainingItemItems()` / `toTrainingTypeLabel()` / `buildCourseSubtitle()`
- 运行 `bash structure_guard_check.sh` 通过
- 运行 `bash structure_smoke_check.sh` 通过

---

## 第 3 步：修 `AccountPage` 的系统能力泄漏

### 本步改动代码
- 新增 `services/user/local/ProfileMediaPickerLocalService.ets`
- 更新 `services/user/index.ets`
- 更新 `pages/profile/AccountPage.ets`

### 解决的问题
- 页面不再直接 import `@ohos.file.photoAccessHelper`
- 页面不再自己实现 `pickPhoto()`
- 页面不再直接调用 `selectAssets()`

### 调整后的边界
- `ProfileMediaPickerLocalService` 统一封装资料页图片选择能力
- `AccountPage` 只保留按钮点击、并发锁、选中结果承接与提示文案
- 系统资源选择异常仍由页面以用户提示语义承接

### 本步自检
- `pages/profile/AccountPage.ets` 中已不存在：
  - `@ohos.file.photoAccessHelper`
  - `selectAssets(`
  - `pickPhoto(`
- 运行 `bash structure_guard_check.sh` 通过
- 运行 `bash structure_smoke_check.sh` 通过

---

## 第 4 步：training 模块收口说明、启动初始化表达与守卫补齐

### 本步改动代码 / 文档
- 新增 `services/training/README.md`
- 更新 `services/training/model/README.md`
- 更新 `services/app/local/AppBootstrapLocalService.ets`
- 更新 `structure_guard_check.sh`
- 更新 `structure_smoke_check.sh`
- 再次更新 `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md`

### 本步结果
- `services/training/README.md` 已明确：
  - `TrainPageService` 是训练页正式门面
  - `TrainPageController` 只做 UI glue
  - `TrainPageRouteAdapter` 只做 `routePath -> routeInput`
  - `TrainPageLocalService` 是动作汇聚点
  - 当前以“冻结扩张、只做收口”为原则
- `AppBootstrapLocalService.ets` 已按模块把初始化项分成：
  - `getCoreInitializers()`
  - `getCourseModuleInitializers()`
  - `getTrainingModuleInitializers()`
  - `getUiInitializers()`
- 新增守卫：
  - `FavoritesPage` 不得重新直读 `UserRepository`
  - `AccountPage` 不得重新直连系统图片选择 API
- 冒烟脚本已补对上述边界与 training README / app bootstrap 分组表达的检查

### 本步自检
- 运行更新后的 `bash structure_guard_check.sh` 通过
- 运行更新后的 `bash structure_smoke_check.sh` 通过

---

## 本轮改动文件清单

### 新增文件
- `services/user/model/FavoriteDisplayItem.ets`
- `services/user/model/README.md`
- `services/user/query/UserFavoritesPageQueryService.ets`
- `services/user/query/README.md`
- `services/user/local/ProfileMediaPickerLocalService.ets`
- `services/training/README.md`
- `docs/restructure/整改执行与自检结果_v41.md`

### 更新文件
- `README.md`
- `pages/README.md`
- `services/README.md`
- `router/README.md`
- `AI_NEXT_HANDOFF_RESTRUCTURE_PLAN.md`
- `services/user/index.ets`
- `pages/profile/FavoritesPage.ets`
- `pages/profile/AccountPage.ets`
- `services/training/model/README.md`
- `services/app/local/AppBootstrapLocalService.ets`
- `structure_guard_check.sh`
- `structure_smoke_check.sh`

---

## 本轮结构收益
- `FavoritesPage` 已从 repository 拉回到 user 模块 query。
- `AccountPage` 已从系统图片选择 API 拉回到 local service。
- training 模块当前边界已文档化，避免继续无意义扩层。
- 启动初始化表达更清晰，但没有引入新的跨层依赖。
- 守卫脚本已覆盖两个高价值页面边界，后续更不容易回退。

---

## 本轮最终自检结果

### `structure_guard_check.sh`
```text
[1] TrainPageService 不再直接解析 routePath
[2] 训练路由解析收口在 TrainPageRouteAdapter
[3] RouteMatcher 只依赖纯路径模式真源
[4] requestApi 不再向上抛出普通网络异常
[5] 训练模块正式出口保持统一
[6] TrainSessionStore 不得混入课程/结果展示字段
[7] api 不得反向依赖 repository/services/pages/storage
[8] TrainSessionStore 与 CourseSessionStore 不得引入 TrainingTypeUtil
[9] pages 不得直连 auth/course/record 零散 PageService
[10] 训练链路公共签名不再扩散 Object
[11] route input 与 bootstrap context 不得回退成裸字符串 stage
[12] 纯请求 service 不得回流旧式 catch
[13] 直接承接纯请求 service 的页面不再保留普通网络失败 catch
[14] 课程与训练项缓存类型不得回退成裸字符串
[15] EntryAbility 不得主动申请训练专属麦克风权限
[16] user api service 不得直接读写 token 或处理上传前置
[17] FavoritesPage 不得重新直读 UserRepository
[18] AccountPage 不得重新直连系统图片选择 API
结构守卫检查通过
```

### `structure_smoke_check.sh`
```text
行为级 smoke check 通过
```

## 本轮故意没动的内容
- 没有继续拆 training 主链路代码，只做边界说明与守卫补齐。
- 没有把 `AppBootstrapLocalService` 提升成更多层级，只做模块分组表达。
- 没有继续扩建 router，只保留并冻结当前结构。
