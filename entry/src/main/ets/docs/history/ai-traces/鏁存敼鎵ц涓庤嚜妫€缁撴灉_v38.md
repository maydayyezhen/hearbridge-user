# 整改执行与自检结果 v38

> 基于 `ets_record_route_cleanup_v37_reworked_all_steps` 按 handoff 文档推荐顺序完成。

## 完成顺序
1. P1-1 清理 `api -> repository` 反向依赖
2. P1-2 前移 store 中残留归一化逻辑
3. P2-1 统一 `auth / course / record` 模块正式出口
4. P2-2 拆分训练链路热点 helper，保持外部入口稳定
5. P3-1 根目录整改过程文档收口到 `docs/restructure/`
6. P3-2 补充守卫与行为冒烟，防止结构回退

## 本轮 README 同步
- `README.md`
- `api/README.md`
- `services/README.md`
- `storage/README.md`

## 本轮新增 helper / index
- `services/auth/index.ets`
- `services/course/index.ets`
- `services/record/index.ets`
- `services/training/local/TrainSessionBootstrapContextLocalService.ets`
- `services/training/local/TrainSessionBootstrapSourceLocalService.ets`
- `services/training/local/TrainSessionBootstrapHydrationLocalService.ets`
- `services/training/local/TrainPracticeUploadLocalService.ets`
- `services/training/local/TrainSessionLifecycleLocalService.ets`

## 最终自检命令
```bash
bash structure_guard_check.sh
bash structure_smoke_check.sh
rg -n "repository/|services/|pages/|storage/" api -g'*.ets'
rg -n "normalizeTrainingType|normalizeTrainItemStatus" storage/train-session storage/course -g'*.ets'
rg -n "services/(auth|course|record)/.*PageService" pages -g'*.ets'
```

## 最终自检结果
见同目录下的 `final_selfcheck_v38.txt` 或本次对话输出。
