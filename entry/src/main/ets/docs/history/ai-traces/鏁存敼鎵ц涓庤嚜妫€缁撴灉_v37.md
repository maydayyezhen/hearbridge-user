# 整改执行与自检结果_v37

说明：

- 本次严格按《项目结构评估整改文档（面向下一位 AI 的执行版）》中的推荐顺序执行，共 5 步。
- 本次做的是**结构整改与静态自检**，**不包含运行时联调和语法编译结果承诺**。
- 最终额外执行：
  - `bash structure_guard_check.sh`
  - `bash structure_smoke_check.sh`

---

## 第一步：先同步 README 与 smoke check

### 已完成改动

调整：

- `repository/README.md`
- `router/README.md`
- `README.md`
- `structure_smoke_check.sh`

### 实际整改内容

1. `repository/README.md` 改为与当前实现一致：
   - `TrainingItemDetailRepository` 只保留基础读写与实体转换说明。
   - `UserRepository` 只保留基础读写与只读事实查询说明。
   - 收藏集合替换/去重/状态回写明确记录为 `services/user/local/UserFavoriteStateService.ets` 负责。

2. `router/README.md` 补充路由解耦约束：
   - RouteMatcher 只依赖纯路径模式。
   - RouteRegistry 只负责渲染绑定。
   - route adapter 不得把 registry 反向拉回。

3. 根 `README.md` 在“结构守卫”下补充“路由匹配与渲染注册必须解耦”的约束。

4. `structure_smoke_check.sh` 改为保护新目标：
   - RouteMatcher 不得再依赖 RouteRegistry。
   - RoutePatternRegistry 不得再从 RouteRegistry 派生。
   - README 不得回退到旧职责表述。

### 自检结果

- `repository/README.md` 中已无旧函数名与“收藏规则仍集中在 repository”旧表述。
- `router/README.md` 已声明匹配与渲染解耦。
- 根 `README.md` 已补路由循环依赖约束。
- `structure_smoke_check.sh` 已改为新目标守卫脚本。

---

## 第二步：处理路由循环依赖

### 已完成改动

调整：

- `router/RouteMatcher.ets`
- `router/RoutePatternRegistry.ets`

### 实际整改内容

1. `RoutePatternRegistry.ets` 不再从 `RouteRegistry` 派生，而是直接基于 `RoutePaths.ets` 构建：
   - `ROUTE_PATTERNS`
   - `ROOT_TAB_PATHS`

2. `RouteMatcher.ets` 改为只依赖：
   - `RoutePatternRegistry`
   - `RouteTypes`

3. 保持 `RouteRegistry.ets` 只负责：
   - `path -> render()` 绑定
   - 根 Tab 展示配置

### 自检结果

- `RouteMatcher.ets` 已移除 `from './RouteRegistry'`。
- `RouteMatcher.ets` 已改为依赖 `ROUTE_PATTERNS`。
- `RoutePatternRegistry.ets` 已不再出现 `ROUTES.map((item) => item.path)`。
- `TrainRootRouteAdapter.ets` 仍只依赖 matcher。

结论：原先的环
`RouteRegistry -> TrainRootPage -> TrainRootRouteAdapter -> RouteMatcher -> RouteRegistry`
已从依赖方向上切断。

---

## 第三步：拆瘦 UserProfileApiService

### 已完成改动

新增：

- `services/user/api/UserActionResult.ets`
- `services/user/local/ProfileMediaUploadLocalService.ets`
- `services/user/api/UserProfileSaveApiService.ets`
- `services/user/api/UserAccountActionService.ets`

调整：

- `services/user/index.ets`

删除：

- `services/user/api/UserProfileApiService.ets`

### 实际整改内容

1. 把资料图片规范化与上传抽到 `ProfileMediaUploadLocalService.ets`：
   - `normalizeProfileImageUri`
   - `getProfileImageFileName`
   - `isRemoteProfileResource`
   - `uploadProfileImageIfNeeded`

2. 新增 `UserProfileSaveApiService.ets`：
   - 抽出 `saveUserProfileInternal(...)` 统一资料保存模板。
   - `saveAvatarProfile / saveCoverProfile / saveUserProfile` 只保留输入准备与调用差异。

3. 新增 `UserAccountActionService.ets`：
   - `changePassword`
   - `logoutCurrentUser`

4. `services/user/index.ets` 改为从新文件统一导出，页面调用入口不变。

### 自检结果

- 旧的 `UserProfileApiService.ets` 已移除。
- 资料保存、账户动作、媒体上传已拆到独立文件。
- `saveUserProfileInternal(...)` 已作为统一模板存在。
- 资料保存文件不再处理 `logout / updatePassword / clearAuthSession`。
- 账户动作文件不再处理图片上传。

---

## 第四步：训练模块轻量收口

### 已完成改动

调整：

- `services/training/TrainPageService.ets`
- `services/training/local/TrainPageLocalService.ets`

删除：

- `services/training/local/TrainSessionFlowLocalService.ets`
- `services/training/local/TrainStageFlowService.ets`

### 实际整改内容

1. `TrainPageService.ets` 改为直接从 `TrainSessionBootstrapLocalService.ets` 引用 `TrainSessionRouteInput`。

2. `TrainPageLocalService.ets` 直接依赖：
   - `TrainSessionBootstrapLocalService`
   - `TrainSessionSubmitFlowLocalService`

3. 训练阶段流转规则并入 `TrainPageLocalService.ets`：
   - `getNextTrainStage`
   - `getPreviousTrainStage`
   - `moveToNextItemOrSummary`
   - `moveToPreviousItemOrIntro`

4. 删除只做转发的 `TrainSessionFlowLocalService.ets`，减少一层无意义跳转。

### 自检结果

- `TrainPageLocalService.ets` 已不再 import `TrainSessionFlowLocalService`。
- `TrainPageLocalService.ets` 已内聚阶段流转规则。
- `TrainPageService.ets` 已直接引用 `TrainSessionBootstrapLocalService`。
- `bash structure_smoke_check.sh` 在本步整改后已通过。

对应的是文档中“删除或收编 TrainSessionFlowLocalService，并把页面阶段流转规则尽量收进 TrainPageLocalService”的要求。

---

## 第五步：debug 状态正规化 + 守卫补强

### 已完成改动

新增：

- `storage/ui/ApiDebugStore.ets`
- `repository/ui/ApiDebugRepository.ets`

调整：

- `api/ApiDebug.ets`
- `services/app/local/AppBootstrapLocalService.ets`
- `structure_guard_check.sh`

### 实际整改内容

1. 新增 `ApiDebugStore.ets`：
   - `initApiDebugStore`
   - `readApiDebugText`
   - `writeApiDebugText`
   - `clearApiDebugText`

2. 新增 `ApiDebugRepository.ets` 作为统一调试文本仓储出口。

3. `api/ApiDebug.ets` 不再直接写 `AppStorage`，改为通过 `writeApiDebugText(...)` 写入。

4. `AppBootstrapLocalService.ets` 不再直接 `AppStorage.setOrCreate('apiDebugText', '')`，改为调用 `initApiDebugRepository()`。

5. `structure_guard_check.sh` 增加新的守卫：
   - RouteMatcher 不得依赖 RouteRegistry
   - services/api 不得裸写 `AppStorage.setOrCreate`
   - repository README 不得回退到旧职责说明

### 自检结果

- `api/ApiDebug.ets` 中已无裸 `AppStorage.setOrCreate(...)`。
- `services/app/local/AppBootstrapLocalService.ets` 中已无裸 `apiDebugText` 初始化。
- `bash structure_guard_check.sh` 通过。
- `bash structure_smoke_check.sh` 通过。

这一步对应文档里“新增 ApiDebugStore / ApiDebugRepository、收掉裸 AppStorage、给守卫脚本加 route matcher / AppStorage / README token 检查”的要求。

---

## 本次最终结论

本轮已经按文档推荐顺序完成 5 步整改，且最终静态守卫结果为：

- `bash structure_guard_check.sh` → **通过**
- `bash structure_smoke_check.sh` → **通过**

### 本轮核心收益

1. 文档、实现、守卫脚本重新对齐，不会再被旧 README 带偏。
2. 路由循环依赖已切断，matcher 与 registry 解耦。
3. 用户资料服务职责拆开，资料保存 / 账户动作 / 媒体上传边界清晰。
4. 训练模块减少了一层无意义中转，阶段流转规则回到训练页本地服务。
5. API debug 状态已正式进入 store / repository 体系，守卫脚本也补上了防回退检查。

### 说明

- 本次未承诺 ArkTS 编译结果，仅完成结构整改与静态自检。
- 若你下一步要继续推进，优先建议直接做一次本地编译 / 页面回归，重点看：
  - 训练页阶段切换
  - 训练列表路由解析
  - 个人资料保存 / 改密 / 登出
  - 调试浮层展示
