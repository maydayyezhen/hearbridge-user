# Train Flow Components

这个目录负责承载训练流程页的页面级 UI 区块组件，避免 `TrainPage.ets` 同时堆叠顶部信息区、动作区、底栏和大纲抽屉的实现细节。

当前主要包含：
- 训练页顶部信息卡片
- 训练页阶段动作条
- 训练页底部栏
- 训练页大纲抽屉

约束：
- 这里只放页面区块组件与展示交互承接
- 不在这里写 repository / store 读写
- 业务编排仍由 page 或 service 层负责
