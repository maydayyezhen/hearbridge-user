# components 目录说明

本目录存放可复用界面组件与局部调试组件。

## 职责
- 提供页面可复用的 UI 结构与展示单元。
- 封装局部交互表现，但不负责完整业务流程。
- 不直接处理跨页面业务编排。

## 约定
- 组件尽量通过参数接收数据与回调。
- 不在组件内部直接调用 repository。
- 若组件内出现明显业务流程，应考虑上提到 `pages` 或 `services`。
